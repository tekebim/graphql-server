# Project to prepare GraphQL Server

### Install dependencies
`npm install`

### Start server
`npm run dev`

Default server endpoint is `app.js`

### Create .env file and fill your own credits
```
MONGODB_USER="username"
MONGODB_PASSWORD="password"
```
